# Copy to /etc/treasuryforge/bitso.env on the VPS and chmod 600.
# NEVER commit the real file. The repo's .gitignore already excludes *.env.
# The validation script / executor read these via resolve_credentials().
BITSO_API_KEY=your_trade_only_no_withdraw_key
BITSO_API_SECRET=your_secret_paste_here
